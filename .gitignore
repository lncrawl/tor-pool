# Go
/torpool
/dist/
*.test
*.out
coverage.*

# Node / dashboard build
node_modules/

web/.vite/
*.tsbuildinfo

# Docs site: .docs-site is staged from README.md and docs/ by
# .github/docs-site.sh, site/ is what mkdocs builds from it.
/.docs-site/
/site/

# Environment / secrets
.env
*.env.local

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local tor state (bind-mounted volume during development)
tor_data/

# Playwright output that is not committed on purpose
web/test-results/

# Built dashboard, embedded at build time. The placeholder index.html is
# committed so `go build` works without running npm.
internal/server/dist/*
!internal/server/dist/index.html
